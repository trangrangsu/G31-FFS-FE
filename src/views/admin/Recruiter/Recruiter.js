import classNames from 'classnames/bind';
import styles from './Recruiter.module.scss';
const cx = classNames.bind(styles);
function Recruiter() {
    return <h2>Recruiter</h2>;
}

export default Recruiter;
