import classNames from 'classnames/bind';

import styles from './Update.module.scss';
const cx = classNames.bind(styles);
function Update() {
    return <div>update recruiter</div>;
}

export default Update;
